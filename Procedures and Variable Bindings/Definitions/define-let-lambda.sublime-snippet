<snippet>
	<content><![CDATA[
(define ${1:name}
  (let ([${2:static-var} ${3:init-value}])
    (labmda (${4:args})
      ${0:body})))
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>define-let-lambda</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.scheme</scope>
	<description>define lambda with local-binding</description>
</snippet>
